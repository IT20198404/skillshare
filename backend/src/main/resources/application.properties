# MongoDB Connection
spring.data.mongodb.uri=mongodb://localhost:27017/skillshare_db

# Multipart upload config
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# OAuth2 Login with Google (replace these)

